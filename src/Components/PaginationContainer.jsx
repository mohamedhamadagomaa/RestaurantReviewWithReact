import React from "react";
import { useLoaderData } from "react-router-dom";
import { RestaurantList, RestaurantsGrid } from "./index";
const PaginationContainer = () => {
  return <>Pagination</>;
};

export default PaginationContainer;
